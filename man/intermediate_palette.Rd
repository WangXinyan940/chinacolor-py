% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7.10 Intermediate.R
\name{intermediate_palette}
\alias{intermediate_palette}
\title{Generate an Intermediate (Square) Color Palette}
\usage{
intermediate_palette(
  base_color = NULL,
  n = 8,
  ctc_colors = TRUE,
  show_pal = TRUE
)
}
\arguments{
\item{base_color}{A character string specifying the base hexadecimal color code
(e.g., "#ECD452"). If \code{NULL}, a random color is selected from the \code{chinacolor} dataset.}

\item{n}{An integer specifying the total number of colors in the palette.
Must be between 4 and 12. Defaults to 8.}

\item{ctc_colors}{A logical value. If \code{TRUE}, the generated colors are mapped
to the closest "Chinese traditional colors" from the \code{chinacolor} dataset.}

\item{show_pal}{A logical value. If \code{TRUE}, the generated palette will be plotted
using \code{\link[chinacolor]{plot_palette}}.}
}
\value{
A character vector of hexadecimal color codes with the following attributes:
\itemize{
\item \code{type}: Character string indicating the palette type, always "qualitative"
\item \code{n}: Number of colors in the palette
\item \code{ctc_colors}: Logical indicating if Chinese traditional color correction was applied
}
}
\description{
This function generates a color palette based on an intermediate color scheme,
specifically a square or tetradic scheme, derived from a base color. It creates four hues
separated by 90-degree intervals (i.e., base hue and +90°, +180°, +270°) on the color wheel.
These four colors form a balanced tetradic (square) color harmony. For each of these four
primary colors, monochromatic variants are generated to expand the palette to the desired
total number of colors (\code{n}). Optionally, the resulting colors can be mapped to the closest
traditional Chinese colors from the \code{chinacolor} dataset.
}
\examples{
\dontrun{
# Generate a 10-color intermediate (square) palette from a specific base color
intermediate_palette(base_color = "#779649", n = 10, ctc_colors = TRUE, show_pal = TRUE)

# Generate an 8-color palette using a random base color
intermediate_palette(n = 8, ctc_colors = TRUE, show_pal = TRUE)
}

}
