% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7.8 compound_concyclic.R
\name{concyclic_palette}
\alias{concyclic_palette}
\title{Generate a Concyclic Color Palette}
\usage{
concyclic_palette(base_color = NULL, n = 8, ctc_colors = TRUE, show_pal = TRUE)
}
\arguments{
\item{base_color}{A character string specifying the base hexadecimal color code
(e.g., "#ECD452"). If \code{NULL}, a random color is selected from the \code{chinacolor} dataset.}

\item{n}{An integer specifying the total number of colors in the palette.
Must be between 4 and 12. Defaults to 8.}

\item{ctc_colors}{A logical value. If \code{TRUE}, the generated colors are mapped
to the closest "Chinese traditional colors" from the \code{chinacolor} dataset.}

\item{show_pal}{A logical value. If \code{TRUE}, the generated palette will be plotted
using \code{\link[chinacolor]{plot_palette}}.}
}
\value{
A character vector of hexadecimal color codes with the following attributes:
\itemize{
\item \code{type}: Character string indicating the palette type, always "qualitative"
\item \code{n}: Number of colors in the palette
\item \code{ctc_colors}: Logical indicating if Chinese traditional color correction was applied
}
}
\description{
This function generates a color palette based on a concyclic color scheme,
where four primary hues are selected from the color wheel at approximately 45-degree
intervals. Specifically, it computes hues at angles \code{base_hue + 45*c(0,1,3,4)},
resulting in a balanced but asymmetric spread: the base color, its adjacent hue (+45°),
a hue in the opposing quadrant (+135°), and its near-complement (+180°).
This arrangement creates visual tension and harmony by spanning nearly half the color wheel
while maintaining structural coherence. For each of these four primary colors,
monochromatic variants are generated to expand the palette to the desired total
number of colors (\code{n}). Optionally, the resulting colors can be mapped to the closest
traditional Chinese colors from the \code{chinacolor} dataset.
}
\examples{
\dontrun{
# Generate a 10-color concyclic palette from a specific base color
concyclic_palette(base_color = "#779649", n = 10, ctc_colors = TRUE, show_pal = TRUE)

# Generate an 8-color palette using a random base color
concyclic_palette(n = 8, ctc_colors = TRUE, show_pal = TRUE)
}

}
